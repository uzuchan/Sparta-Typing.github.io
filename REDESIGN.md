# Sparta Typing — ゲーム性 再設計ドキュメント

> 実プレイ体験（`memo.md`）を起点とした設計見直し。
> 目的は「破綻しているスパルタモード」を成立させ、答えリークを解消し、
> 音ゲー的な手応えのある体験へ作り替えること。

---

## 0. このドキュメントの位置づけ

- これは **設計提案** であり、実装はまだ行っていません。
- 各節に現状コードの該当箇所（`file:line`）を併記し、変更点を追跡可能にしています。
- 「いま入れる（Phase 1）」「あとで入れる（Phase 2 以降）」を §11 で切り分けています。

---

## 1. 現状の問題点（コード根拠つき）

| # | 問題 | 根拠 | 影響 |
|---|------|------|------|
| P1 | **答えが常に見えている** | `WordCard.tsx:53-55` の `sub-display` がモード無関係に答えを表示（en→ja=日本語表記、ja→en=英単語） | 暗記テストとして成立しない |
| P2 | **スパルタが速すぎて打ち切れない** | `scoring.ts:24-27` `calcSpartaLimitMs = min(3.0, 2.0 + len*0.05)` → 全単語 **2〜3秒固定** | ゲームが破綻 |
| P3 | **モードの差が「速度」だけ** | Practice/Sparta で時間式が別物（`time.ts:3` vs `scoring.ts:24`）。表示は同じ | スパルタの本質（記憶での想起）が表現されていない |
| P4 | **打った文字が消える** | `WordCard.tsx:57-70` で入力進捗を都度書き換え、境界（判定ライン）がない | リズムが取れず爽快感がない |
| P5 | **HP が常時 ON で固定** | `gameStore.ts:39` `STARTING_HP=3`、`:291-294` ミスで減少 | 初見殺し。気軽に遊べない |
| P6 | **制限時間が体感とズレる** | Practice でも `6 + len*0.45`（最大15秒）。「分かっているのに間に合わない」 | ストレス |
| P7 | **カタカナの読みが打てない** | `romaji.ts` の `ROMAJI_MAP` がひらがな中心。`kanaToRomajiCandidates` がカタカナを正規化せず、未対応文字はそのまま候補化される。`useTypingInput.ts:12-15` は en→ja をローマ字入力のみに制限 | 外来語・固有名詞などカタカナ回答で正解できず、学習データを安心して増やせない |

---

## 2. 再設計の中心アイデア

### 2つの軸を分離する

現状は「Practice か Sparta か」で **速度・HP・表示** がまとめて切り替わっている。
これを **独立した2軸** に分解する:

```
軸A: 出題形式 (reveal mode)
    READ   … 答え（打つべき文字）が見えている。読んで打つ。  → Practice
    RECALL … 答えは隠れている。記憶から打つ。時間が迫ると出る。 → Sparta

軸B: 速度 (speed / 制限時間)
    デフォルトは Practice / Sparta 共通の「ゆとりある時間」。
    速度は将来ユーザーが選べる独立設定（§9）。
```

これにより memo の要求が素直に満たされる:

- 「スパルタの時間はデフォルトを Practice と同じに」 → **軸Bの初期値を共通化**。スパルタの違いは「答えを隠す」ことだけ。
- 「Practice は表示文字を打つ／Sparta は隠して時間が迫ると出す」 → **軸A** そのもの。
- 「HP はデフォルト削除、選べるように」 → **§8 のオプション**（軸とは別の独立フラグ）。

> 要するに **「スパルタ＝速い」ではなく「スパルタ＝思い出して打つ」** に再定義する。
> 速さは後から好みで足す“トッピング”にする。

---

## 3. モード設計

### 3.1 Practice（READ / 練習）
- 答え（打つべき文字列）は **最初から見えている**。
- 制限時間は **ゆとりベース**（§5）。コンボ倍率は維持（`scoring.ts:66`）。
- HP なし（もともと無し）。セッション時間制（従来どおり 60/120/180/300 秒）。
- 目的: 綴り・読みを覚える、運指を慣らす。

### 3.2 Sparta（RECALL / 暗記）
- 答えは **隠れている**。プレイヤーは記憶から打つ。
- **デフォルトの制限時間は Practice と同じ**（軸B共通値）。
- 時間が一定割合を過ぎると **段階的に答えが出てくる救済**（§6）。ただし出た瞬間にスコアは下がる/0 になる（`scoring.ts:37` の既存ルールを踏襲）。
- **HP はデフォルト OFF**（§8）。ミスしても即終了しない。
- 目的: 「見ずに思い出せるか」を測る。スコア＝想起の速さと正確さ。

### 3.3 Endless（耐久）
- 形式は RECALL ベースだが、5問ごとに **速度（軸B）** を少しずつ詰める（`gameStore.ts:40-41` の既存 tighten ロジックを軸Bに接続）。
- 終了条件はユーザー操作のみ（従来どおり）。

> モード選択 UI（`PlayPage.tsx:13-41`）の説明文を上記に合わせて更新する。
> 特に Sparta の「HP3・短い制限時間」という記述（`:32`）は撤回する。

---

## 4. 出題形式（軸A）の状態モデル

`GameQuestion` に「答えを今どこまで見せるか」を表す `revealMode` を持たせ、
表示は **時間経過に対する reveal スケジュール** で決める。

```ts
type RevealMode = "read" | "recall";

// 既存 HintLevel ("none" | "first_char" | "full") を reveal の段階として流用する
// READ:   最初から full
// RECALL: none → first_char → full と時間で昇格（救済）
```

- READ は `getHintLevel` を無視して常に `full` 相当。
- RECALL は既存 `getHintLevel`（`time.ts:9-15`）の閾値を流用しつつ、**閾値を後ろ倒し**にする（救済は終盤だけにしたいので §6）。

---

## 5. タイミングモデル（軸B / 制限時間の新計算式）

memo:「1秒で分かったら +10秒くらい猶予」= **理解 1秒 + 打鍵時間 + バッファ** を基本に。

```ts
// 提案: lib/time.ts に統合（calcSpartaLimitMs は廃止）
function calcTimeLimitMs(targetLen: number, opts: {
  speedFactor?: number;   // 軸B。1.0 = 標準（デフォルト）。小さいほど短い＝難しい
}): number {
  const recognitionMs = 1000;                 // 意味を思い出す ~1s
  const typingMs      = targetLen * 400;       // 1文字 ~0.4s（標準運指）
  const bufferMs      = 8000;                  // ゆとり ~8s（"+10s 猶予"の主成分）
  const raw = (recognitionMs + typingMs + bufferMs) * (opts.speedFactor ?? 1);
  return Math.round(clamp(raw, 6000, 20000));  // 6〜20s
}
```

例（標準 speedFactor=1.0）:

| 答えの長さ | 制限時間 |
|---|---|
| 3文字 | 約 10.2s |
| 6文字 | 約 11.4s |
| 10文字 | 約 13.0s |

- **Practice / Sparta どちらもこの式を共通利用**（P2/P3 解消）。
- スパルタ専用の `calcSpartaLimitMs`（`scoring.ts:24`）は **削除**。
- スコア計算（`calcSpartaScore` `scoring.ts:29`）は `readingLength` ではなく
  この統一 `timeLimitMs` を基準に ratio を取るよう修正（速度が変わってもスコアが破綻しないように）。

> 将来 `speedFactor` をユーザー設定／自動提案で動かす（§9）。
> いまは UI に出さず 1.0 固定でよい。

---

## 6. RECALL の答えリビール（救済）と P1（リーク）修正

### 6.1 答えリークの修正
`WordCard.tsx:53-55` の `sub-display`（常時表示）が諸悪の根源。**reveal 状態に従わせる**:

- READ: 打つべき文字（en→ja は読みカナ、ja→en は英単語）を表示してよい。
- RECALL: `reveal === "none"` の間は **答え・読み・英単語をすべて隠す**。
  画面に出るのは「お題（問題文）」と入力中の自分の打鍵だけ。

| 方向 | お題（常時表示） | 打つ対象 | RECALL中の隠す対象 |
|---|---|---|---|
| en→ja | 英単語 | 読みカナ | カナ・日本語表記 |
| ja→en | 日本語表記 | 英単語 | 英単語 |

### 6.2 リビール・スケジュール（RECALL のみ）
落下ノーツ（§7）が判定ラインに近づくほど答えが出る = 終盤救済。

```
elapsed/limit < 0.70 : none        （完全に記憶頼み。満点ゾーン）
0.70 〜 0.88        : first_char    （頭文字だけ薄く。減点）
0.88 〜 1.00        : full          （全部出る。最低点 or 0）
```

- `time.ts:9-15` の閾値（0.35 / 0.65）を上記（0.70 / 0.88）へ変更。
  READ では常に full なので影響なし、RECALL だけ終盤救済になる。
- スコア連動は既存ルールを流用: ヒントが出たら sparta スコア 0（`scoring.ts:37`）。

### 6.3 カタカナ回答の入力対応（P7）
en→ja の打つ対象は「読みカナ」なので、ひらがな・カタカナが混在しても同じローマ字入力で正解できるようにする。

- `kanaToRomajiCandidates` の入口で **カタカナをひらがなへ正規化**してから候補生成する。
  - 例: `コンピューター` → `こんぴゅーたー` → `konpyu-ta-` / `computer` ではなく読みのローマ字で判定。
  - `ヴァ/ヴィ/ヴ/ヴェ/ヴォ`、`ティ/ディ/ファ/フィ/フェ/フォ` など外来語で頻出する拗音も `ROMAJI_MAP` に追加する。
- 表示は元の `answerDisplay` / `answerReading` を維持する。**入力判定だけ正規化**し、画面上のカタカナ表記を勝手にひらがなへ崩さない。
- `parseAnswerRaw` の運用ルールとして、表示と読みを分けたい語は `表示:読み` を使う。
  - 例: `PC:ピーシー`、`Wi-Fi:ワイファイ`。
- IME で直接カタカナを入れる方式は Phase 1 では必須にしない。まずは既存のローマ字入力体験を壊さず、カタカナ読みでも通るようにする。

---

## 7. 音ゲー風「落下ノーツ」UI（P4 解消）

memo:「境界を決めて、音ゲーでノーツが落ちるみたいに。打っている文字が消えないように」

### 7.1 レイアウト

```
┌──────────────────────────────┐
│            お題: "apple"        │  ← 上部に問題文（固定）
│                                │
│            ▼ ノーツ              │  ← ここを time 経過で下に落下
│            ▼                    │
│         （RECALL中は中身を伏せる）  │
│                                │
│ ━━━━━━━━━ 判定ライン ━━━━━━━━━ │  ← 境界。ここに到達＝time up
│      [ a p p l e ]   ← 入力済み  │  ← 打鍵は“ラインに焼き付く”＝消えない
└──────────────────────────────┘
```

### 7.2 仕様
- ノーツ（お題カード）は出題時に上端、`elapsed/limit` で判定ラインまで線形落下。
  - 既存の `--fall`（`WordCard.tsx:46`）と `FallBar`（`GameScreen.tsx:123-142`）の ratio をそのまま「落下位置」に転用。
  - `FallBar`（横ゲージ）は判定ラインの“予告”として残してもよいし、ノーツ落下に統合してもよい。
- **判定ラインは固定**。プレイヤーの入力済み文字は **ラインの直上に常設の枠** として表示し、打つたびに左→右へ確定。**一度打った文字は消さない**（P4 の核心）。
- ノーツが判定ラインに触れる＝ `onTimeUp`（`gameStore.ts:312`）。
- RECALL ではノーツ中身は伏せ字（●●●…）。リビール段階（§6.2）に応じて中身が浮かび上がる。
- 正解時: ノーツが弾けるエフェクト＋スコアポップ（既存 `FeedbackLayer` 流用）。

### 7.3 入力済み文字を「焼き付ける」表示
- 現状 `computeDoneLength`（`WordCard.tsx:99-128`）でカナ進捗を推定 → これを判定ライン上の確定スロットに反映。
- en→ja のローマ字途中入力（`romaji-progress` `WordCard.tsx:72-77`）も判定ライン付近に固定表示し、確定したカナはスロットに積む。

---

## 8. HP・難易度オプション（P5 解消）

### 8.1 デフォルト
- **HP は OFF**（ミスでも体力は減らない。即終了しない）。
  - `gameStore.ts:291-294`（ミス時 `loseHp`）と `:316-320`（time up 時 `loseHp`）を **設定フラグでガード**。
  - HP OFF 時の Sparta 終了条件は「セッション時間制」または「ブロック一巡」または手動終了に変更（`gameStore.ts:586-591` の終了判定を更新）。

### 8.2 ユーザー設定（`UserPref` に保存）
既存 `UserPref`（`types.ts:103-106`）／`db` に永続化。

```ts
type GameOptions = {
  revealMode: "read" | "recall";   // モードのデフォルトに連動。上書き可
  speedFactor: number;             // 軸B。既定 1.0
  hpEnabled: boolean;              // 既定 false
  startingHp: number;              // hpEnabled 時のみ。既定 3
};
```

- モード選択画面（`PlayPage.tsx`）に「HP あり/なし」「速度」のトグルを追加。
- 既定値で起動すれば、初見でも気軽に遊べる。

---

## 9. 将来拡張: タイピング速度の計測と最適速度提案

memo:「タイピング速度を測り、最適速度を提案、ユーザーが選べる」

### 9.1 データはすでにある
- `WordStats.avgLatencyMs`（`types.ts:49`）= 単語ごとの平均解答時間。
- `Attempt.elapsedMs` / `timeLimitMs`（`types.ts:75-77`）= 1試行ごとの実測。
  → これらから **個人の打鍵速度（ms/文字）** が推定できる。

### 9.2 提案ロジック（案）
1. Practice（READ）の試行から `ms/char` の中央値を算出（理解時間と打鍵時間を分離するため、READ を基準に）。
2. 「正答の ~80% が時間内に収まる」ような `speedFactor` を逆算して **推奨値** とする。
3. 結果画面・設定に「あなたの推奨速度: ×0.8（やや速い）」のように提示し、スライダーで選択可能に。

### 9.3 キャリブレーション・セッション（任意）
- 初回や任意で「速度測定モード」を用意（既知の短文を打たせて KPM を測る）。
- 測定値を `UserPref` に保存し、以後の `speedFactor` 提案の初期値にする。

---

## 10. 実装影響マップ

| ファイル | 変更内容 |
|---|---|
| `src/lib/time.ts` | `calcTimeLimitMs` を統一式へ（§5）。`getHintLevel` 閾値を 0.70/0.88 に（§6.2） |
| `src/lib/scoring.ts` | `calcSpartaLimitMs` 削除。`calcSpartaScore` を統一 `timeLimitMs` 基準に |
| `src/lib/romaji.ts` | カタカナ→ひらがな正規化を追加。外来語向けの `ヴァ/ティ/ファ` 系などを `ROMAJI_MAP` に追加（§6.3） |
| `src/lib/answerParser.ts` | `表示:読み` の運用を前提に、カタカナ読みでも `answerReading` が判定対象になることを確認（必要ならテスト追加） |
| `src/features/game/engine.ts` | `buildQuestion` の time 分岐（`:21-28`）を統一式に。`revealMode` を付与 |
| `src/types.ts` | `GameQuestion` に `revealMode`、`GameOptions`/`UserPref` 拡張 |
| `src/features/game/gameStore.ts` | HP をオプション化（`:39,:291-320,:586-591`）。終了条件見直し。options 読み込み |
| `src/features/game/WordCard.tsx` | sub-display のリーク修正（`:53-55`）。RECALL 伏せ字＋リビール。判定ライン確定表示 |
| `src/features/game/GameScreen.tsx` | `FallBar` を判定ライン／落下ノーツに発展（`:123-142`） |
| `src/features/game/Hud.tsx` | HP 表示を `hpEnabled` で出し分け（`:73-91`） |
| `src/pages/PlayPage.tsx` | モード説明文更新（`:13-41`）。HP/速度トグル追加 |
| `src/styles/*` | 判定ライン・落下ノーツ・伏せ字のスタイル追加 |

---

## 11. 段階導入プラン

### Phase 1 — 「破綻」を直す（最優先・小さく速く）
1. **P1 リーク修正**: RECALL 中は答えを隠す（`WordCard` sub-display）。
2. **P7 カタカナ入力修正**: カタカナ読みをひらがなへ正規化し、ローマ字入力で正解できるようにする（§6.3）。
3. **P2/P3 速度修正**: 時間式を統一（Sparta = Practice 既定）。`calcSpartaLimitMs` 廃止。
4. **P5 HP**: デフォルト OFF（フラグ追加、Sparta 終了条件を時間/手動に）。
5. リビール閾値を終盤寄りに（§6.2）。

→ この5点だけで「速すぎる・答えバレ・カタカナが打てない・初見殺し」は解消し、遊べる状態になる。

### Phase 2 — 手触り（音ゲー化）
6. 判定ライン＋落下ノーツ UI（§7）。打鍵を消さない確定表示。
7. 伏せ字→リビールの視覚演出。

### Phase 3 — パーソナライズ
8. HP/速度のユーザー設定 UI（§8）。
9. タイピング速度計測と推奨 `speedFactor`（§9）。

---

## 12. 未確定・要相談ポイント

- 統一時間式の係数（`typingMs = len*400`, `buffer=8000`）は **要チューニング**。実測で調整。
- Sparta の HP OFF 時の終了条件: 「セッション時間制」にするか「ブロック一巡で終了」にするか。
- リビールでスコア 0 のままにするか、`first_char` は一部得点を残すか（救済の重み）。
- 落下ノーツは1問1ノーツか、文字ごとにノーツを落とす本格音ゲー型か（実装コスト大）。
- カタカナ入力は Phase 1 ではローマ字入力対応を優先。将来、IME で直接「カタカナ/ひらがな」を打つ方式も許可するかは別途検討。
